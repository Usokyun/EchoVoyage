export type TrainingSceneProps = {
  title: string
  prompt: string
  targetLandmarkIds: string[]
}

export function TrainingScene(_props: TrainingSceneProps) {
  // Future UI: roleplay dialogue, answer input, and Agent feedback.
  return null
}
