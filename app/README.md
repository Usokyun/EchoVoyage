# App

Future Next.js App Router pages live here.

Day 1 keeps this as a skeleton only. The target routes are:

- `/`: landing page, corpus input, Agent selector, and static voyage preview
- `/voyage`: generated Journey Map and current training scene
- `/review`: today's echoes and review return flow
